#!/bin/sh
mkdir -p /opt/etc/dns-monitor
: > /opt/etc/dns-monitor/profiling.enabled
if [ -x /opt/etc/init.d/S90dns-monitor ]; then
    /opt/etc/init.d/S90dns-monitor restart >/dev/null 2>&1 || true
fi
exit 0
