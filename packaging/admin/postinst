#!/bin/sh
mkdir -p /opt/var/run
if [ -x /opt/etc/init.d/S91routerforge-admin ]; then
    /opt/etc/init.d/S91routerforge-admin stop >/dev/null 2>&1 || true
    /opt/etc/init.d/S91routerforge-admin start >/dev/null 2>&1 || true
fi
exit 0
